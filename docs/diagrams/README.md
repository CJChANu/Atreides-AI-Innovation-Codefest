# Diagrams

- `architecture.mmd` — component and data flow (Mermaid)
- `investigation-loop.mmd` — the bounded 1C state machine (Mermaid)

Both render directly on GitHub. To export locally:

```bash
npx @mermaid-js/mermaid-cli -i architecture.mmd -o architecture.png
```
