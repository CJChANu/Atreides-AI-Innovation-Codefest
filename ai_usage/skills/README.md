# Skills

Reusable, repository-specific instructions given to AI coding assistants, kept
here so the way we directed the tooling is part of the submission record (§4.1).

| File | Purpose |
|---|---|
| `verify-against-corpus.md` | Require any claim about the archive to be checked against the archive before it is written down |

See `../claude.md` for the standing repository instructions and `../context.md`
for the project context supplied at the start of each session.
